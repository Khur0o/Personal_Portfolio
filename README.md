# Personal Portfolio

This project uses React with Vite and plain JavaScript.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run preview`

## Notes

The app is configured for a JavaScript-based frontend and does not include TypeScript tooling.
